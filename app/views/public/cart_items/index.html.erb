<h3>ショッピングカート</h3>

<table border="1" cellpadding="5">
  <thead>
    <tr>
      <td>商品名</td>
      <td>単価（税込）</td>
      <td>数量<%= link_to "変更", method: :patch, class: 'btn btn-success' %></td>
      <td>小計</td>
      <td></td>
    </tr>
  </thead>

  <tbody>
    <tr>
      <%= @items.each do |item| %>
      <%= @cart_items.each do |cart_item| %>
      <td><%= item.item_id %></td>
      <td><%= (cart_item.item.with_tax_price %></td>
      <td><%= item.amount %></td>
      <td><%= cart_item.subtotal %></td>
      <%= @total_item_price += cart_item.subtotal %>
      <td>
        <%= link_to "削除する", public_cart_item_path, class: 'btn btn-danger', method: :delete %>
      </td>
      <% end %>
      <% end %>
    </tr>
  </tbody>
</table>

<%= link_to "買い物を続ける", public_items_path, class: 'btn btn-primary' %>

      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <%= link_to "情報入力に進む", new_public_order_path, class: 'btn btn-success' %>
          </div>
        </div>
      </div>
